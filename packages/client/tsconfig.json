{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "paths": {
      "@tg-search/core": ["../../packages/core/src"],
      "@tg-search/common": ["../../packages/common/src"],
      "@tg-search/client": ["../../packages/client/src"]
    },
    "types": [
      "vite/client",
      // TODO: Remove this once we have a better solution for node types
      "node"
    ]
  },
  "include": [
    "./src/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
